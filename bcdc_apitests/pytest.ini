[pytest]
log_format = %(asctime)s - %(lineno)s - %(name)s - %(levelname)s - %(message)s
log_date_format = %Y-%m-%d %H:%M:%S
log_cli=true
log-cli-level=DEBUG
log_level=10

testpaths=.
rootdir=.
norecursedirs=ve src *.egg dist build docs secrets
